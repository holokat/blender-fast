interface:
  display_name: "Blender fast"
  short_description: "Batch Blender work and verify the visual result"
  default_prompt: "Use $blender-fast to build or edit this Blender scene with batched operations and visual verification."
policy:
  allow_implicit_invocation: true
