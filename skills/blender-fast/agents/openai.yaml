interface:
  display_name: "Blender fast"
  short_description: "Optimize Blender builds and renders with measured settings"
  default_prompt: "Use $blender-fast to build and render this scene with the measured combined workflow, preserving the requested quality and verifying the result."
policy:
  allow_implicit_invocation: true
